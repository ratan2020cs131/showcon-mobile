const express = require('express');
const route = express.Router();
const { AuthMiddleWare } = require('../middleware/AuthMiddleware.js')
const { } = require('../controller/CinemaController.js');


route.get('/:id', );

module.exports = route;